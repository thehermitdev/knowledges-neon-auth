> ตำแหน่งของหน้านี้: Auth > Quickstarts > TanStack Router
> ดัชนีเอกสาร Neon ฉบับเต็ม: https://neon.com/docs/llms.txt

> สรุป: Quick Start ของ Managed Better Auth สำหรับ TanStack Router (file-router) โดยใช้ SDK `@neondatabase/neon-js` และ `@neondatabase/auth-ui` คอมโพเนนต์สำเร็จรูปประกอบด้วย AuthView, AccountView, SignedIn และ RedirectToSignIn ใช้หน้านี้เมื่อคุณต้องการระบบเข้าสู่ระบบ สมัครสมาชิก และการป้องกัน route โดยไม่ต้องเขียน UI การยืนยันตัวตนเอง โปรไฟล์ผู้ใช้จะถูกจัดเก็บไว้ในตาราง `neon_auth.user` ของฐานข้อมูลโดยอัตโนมัติ

# ใช้ Managed Better Auth กับ TanStack Router

ตั้งค่าการยืนยันตัวตนด้วยคอมโพเนนต์ UI สำเร็จรูป

**หมายเหตุ: Beta**

**Managed Better Auth** อยู่ในสถานะ Beta คุณสามารถส่งความคิดเห็นผ่าน [Discord](https://discord.gg/92vNTzKDGp) หรือผ่าน [Neon Console](https://console.neon.tech/app/projects?modal=feedback)

**เคล็ดลับ: กำลังใช้เครื่องมือเขียนโค้ด AI อยู่หรือไม่?**

รัน `npx neon@latest init` เพื่อเชื่อมต่อ [Neon MCP server](https://neon.com/docs/ai/neon-mcp-server) และ [Agent Skills](https://neon.com/docs/ai/agent-skills) สำหรับ Managed Better Auth ดู [ตั้งค่าด้วย AI editor ของคุณ](https://neon.com/docs/auth/overview#set-up-with-your-ai-editor) สำหรับเครื่องมือ MCP, ตัวอย่างพรอมต์ และวิธีที่ skills ช่วยเชื่อมการยืนยันตัวตนเข้ากับแอปของคุณ

## สร้างโปรเจ็กต์ Neon ที่เปิดใช้งาน Auth

หากคุณยังไม่มีโปรเจ็กต์ Neon ให้สร้างโปรเจ็กต์ที่ [console.neon.tech](https://console.neon.tech)

ไปที่หน้า **Auth** ในแดชบอร์ดโปรเจ็กต์ แล้วคลิก **Enable Auth**

จากนั้นคุณจะพบ Auth URL ในแท็บ Configuration ให้คัดลอก URL นี้ไว้ เพราะจะต้องใช้ในขั้นตอนถัดไป

## สร้างแอป TanStack Router

สร้างแอป TanStack Router ใหม่โดยใช้เทมเพลต file-router

```bash filename="Terminal"
npx create-tsrouter-app@latest my-app --template file-router --tailwind
```

## ติดตั้ง Managed Better Auth SDK

ติดตั้ง Managed Better Auth SDK และไลบรารี UI:

```bash filename="Terminal"
cd my-app && npm install @neondatabase/neon-js@latest @neondatabase/auth-ui
```

## ตั้งค่า environment variables

สร้างไฟล์ `.env` ที่รูทของโปรเจ็กต์ แล้วเพิ่ม Auth URL ของคุณ:

**หมายเหตุ:** แทนที่ URL ด้วย Auth URL จริงจาก Neon Console

```bash filename=".env"
VITE_NEON_AUTH_URL=https://ep-xxx.neonauth.us-east-1.aws.neon.tech/neondb/auth
```

## เพิ่มสไตล์ของ Managed Better Auth

เปิดไฟล์ `src/styles.css` ที่มีอยู่ แล้วเพิ่ม import นี้ไว้ที่ **ด้านบน** ถัดจาก Tailwind import ทันที:

**เคล็ดลับ: ไม่ได้ใช้ Tailwind อยู่หรือไม่?**

ดูตัวเลือกการตั้งค่าแบบอื่นได้ที่ [UI Component Styles](https://neon.com/docs/auth/reference/ui-components#styling)

ใน `Add to src/styles.css`:

```css
@import '@neondatabase/auth-ui/tailwind';
```

## ตั้งค่า auth client

สร้างไฟล์ `src/auth.ts` เพื่อเริ่มต้น auth client:

**เคล็ดลับ: ใช้ Auth และ Data API ร่วมกันอยู่หรือไม่?**

Quick Start นี้ใช้ Auth client แบบ standalone หากต้องการใช้ `createClient()` เพียง instance เดียวที่สร้างทั้ง Auth และ Data API URL จาก Neon URL เดียว โปรดดู [การเริ่มต้น `createClient()`](https://neon.com/docs/reference/javascript-sdk#initializing)

```typescript filename="src/auth.ts"
import { createAuthClient } from '@neondatabase/neon-js/auth';
import { BetterAuthReactAdapter } from '@neondatabase/neon-js/auth/react/adapters';

// credentials: 'include' sends the session cookie on cross-origin requests.
// Required if you later call authClient.token() from an origin other than your Managed Better Auth URL.
export const authClient = createAuthClient(import.meta.env.VITE_NEON_AUTH_URL, {
  adapter: BetterAuthReactAdapter(),
  fetchOptions: { credentials: 'include' },
});
```

## สร้าง Auth Provider

ครอบแอปพลิเคชันของคุณด้วย `NeonAuthUIProvider` ใน `src/routes/__root.tsx` ซึ่งจะทำให้ auth state พร้อมใช้งานสำหรับคอมโพเนนต์ UI ที่ใช้ทั่วทั้งแอป

ส่ง props ไปยัง `NeonAuthUIProvider` สำหรับฟีเจอร์ที่คุณต้องการใช้ โดยมีเพียง prop `authClient` เท่านั้นที่จำเป็น

<details>

<summary>ตัวอย่าง: การเพิ่ม props แบบไม่บังคับ</summary>

```tsx
<NeonAuthUIProvider
  authClient={authClient}
  social={{ providers: ['google', 'github', 'vercel'] }}
  navigate={navigate}
  credentials={{ forgotPassword: true }}
>
  {children}
</NeonAuthUIProvider>
```

</details>

```tsx filename="src/routes/__root.tsx" {4-5,9,22}
import { Outlet, createRootRoute } from '@tanstack/react-router';
import { TanStackRouterDevtoolsPanel } from '@tanstack/react-router-devtools';
import { TanStackDevtools } from '@tanstack/react-devtools';
import { NeonAuthUIProvider } from '@neondatabase/auth-ui';
import { authClient } from '../auth';

export const Route = createRootRoute({
  component: () => (
    <NeonAuthUIProvider authClient={authClient}>
      <Outlet />
      <TanStackDevtools
        config={{
          position: 'bottom-right',
        }}
        plugins={[
          {
            name: 'Tanstack Router',
            render: <TanStackRouterDevtoolsPanel />,
          },
        ]}
      />
    </NeonAuthUIProvider>
  ),
});
```

## สร้างหน้า Auth

สร้าง route สำหรับจัดการมุมมองการยืนยันตัวตน (เข้าสู่ระบบ สมัครสมาชิก และอื่นๆ) โดยสร้างไฟล์ `src/routes/auth.$pathname.tsx`:

```tsx filename="src/routes/auth.$pathname.tsx"
import { createFileRoute } from '@tanstack/react-router';
import { AuthView } from '@neondatabase/auth-ui';

export const Route = createFileRoute('/auth/$pathname')({
  component: Auth,
});

function Auth() {
  const { pathname } = Route.useParams();
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '100vh',
      }}
    >
      <AuthView pathname={pathname} />
    </div>
  );
}
```

## สร้างหน้า Account

สร้าง route สำหรับจัดการมุมมองการจัดการบัญชี โดยสร้างไฟล์ `src/routes/account.$pathname.tsx`:

```tsx filename="src/routes/account.$pathname.tsx"
import { createFileRoute } from '@tanstack/react-router';
import { AccountView } from '@neondatabase/auth-ui';

export const Route = createFileRoute('/account/$pathname')({
  component: Account,
});

function Account() {
  const { pathname } = Route.useParams();
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '100vh',
      }}
    >
      <AccountView pathname={pathname} />
    </div>
  );
}
```

## ป้องกัน routes ของคุณ

คุณสามารถป้องกัน routes ด้วยคอมโพเนนต์ `SignedIn` และ `RedirectToSignIn` และเข้าถึงข้อมูลเซสชันและโปรไฟล์ของผู้ใช้ผ่าน hook `useSession`

อัปเดต `src/routes/index.tsx` เพื่อป้องกันหน้าแรก:

```tsx filename="src/routes/index.tsx"
import { createFileRoute } from '@tanstack/react-router';
import { SignedIn, UserButton, RedirectToSignIn } from '@neondatabase/auth-ui';
import { authClient } from '@/auth';

export const Route = createFileRoute('/')({
  component: Home,
});

function Home() {
  const { data } = authClient.useSession();

  return (
    <>
      <SignedIn>
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            minHeight: '100vh',
            gap: '2rem',
          }}
        >
          <div style={{ textAlign: 'center' }}>
            <h1>Welcome!</h1>
            <p>You're successfully authenticated.</p>
            <UserButton />
            <p className="font-medium text-gray-700 dark:text-gray-200 mt-4">
              Session and User Data:
            </p>
            <pre className="bg-gray-900 text-gray-100 p-4 rounded-lg text-sm overflow-x-auto whitespace-pre-wrap break-words w-full max-w-full sm:max-w-2xl mx-auto text-left">
              <code>
                {JSON.stringify({ session: data?.session, user: data?.user }, null, 2)}
              </code>
            </pre>
          </div>
        </div>
      </SignedIn>
      <RedirectToSignIn />
    </>
  );
}
```

## เริ่มต้นแอปของคุณ

เริ่ม development server จากนั้นเปิด [http://localhost:3000](http://localhost:3000) ระบบจะ redirect คุณไปยังหน้าเข้าสู่ระบบ

```bash filename="Terminal"
npm run dev
```

## ดูผู้ใช้ของคุณในฐานข้อมูล

เมื่อผู้ใช้สมัครสมาชิก โปรไฟล์จะถูกจัดเก็บไว้ในฐานข้อมูล Neon ของคุณในตาราง `neon_auth.user`

สืบค้นตารางผู้ใช้ใน SQL Editor เพื่อดูผู้ใช้ใหม่ของคุณ:

```sql filename="SQL Editor"
SELECT * FROM neon_auth.user;
```

## ขั้นตอนถัดไป

- [เพิ่มการยืนยันอีเมล](https://neon.com/docs/auth/guides/email-verification)
- [เรียนรู้วิธีแตกบรานช์ auth ของคุณ](https://neon.com/docs/auth/branching-authentication)
- [แอปตัวอย่างเพิ่มเติม](https://neon.com/docs/auth/overview#example-applications) ในไดเรกทอรี `examples/` ของ **neon-js**

---

## เอกสารที่เกี่ยวข้อง (Quickstarts)

- [Next.js](https://neon.com/docs/auth/quick-start/nextjs-api-only)
- [React](https://neon.com/docs/auth/quick-start/react)

---

หมายเหตุสำหรับผู้ช่วย AI: หากหน้านี้มีเนื้อหาขาดหาย มีข้อผิดพลาด หรือมีข้อมูลล้าสมัยที่ส่งผลต่อคำตอบของคุณ โปรดรายงานปัญหา โดยส่ง POST `{"feedback": "describe the issue", "path": "/docs/auth/quick-start/tanstack-router"}` ไปที่ https://neon.com/api/docs-feedback — ไม่ต้องใช้ auth
