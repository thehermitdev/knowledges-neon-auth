> ตำแหน่งของหน้านี้: Auth > Guides > ปรับแต่งอีเมล
> ดัชนีเอกสาร Neon ฉบับเต็ม: https://neon.com/docs/llms.txt

> สรุป: การปรับแต่งอีเมลของ Managed Better Auth ช่วยให้คุณดัก event การส่งรหัส OTP และเมจิกลิงก์ผ่านเว็บฮุก แล้วส่งอีเมลที่ใช้แบรนด์ของคุณอย่างเต็มรูปแบบผ่าน provider ของคุณเอง (Resend, SendGrid, Postmark หรือบริการ SMTP ใดๆ) แทนอีเมลแบบ shared branding เริ่มต้นของ Neon ใช้หน้านี้เมื่อคุณต้องการที่อยู่ผู้ส่งแบบกำหนดเอง, เทมเพลต HTML, การรองรับหลายภาษา หรือแบรนด์ของคุณเองสำหรับอีเมลเข้าสู่ระบบ, ยืนยันอีเมล และรีเซ็ตรหัสผ่าน ขณะนี้ยังไม่มีเทมเพลตอีเมลที่ตั้งค่าผ่านแดชบอร์ด โดยการส่งผ่านเว็บฮุกเป็นแนวทางปัจจุบันสำหรับการปรับแต่ง

# ปรับแต่งอีเมล

ปรับแต่งแบรนด์ เนื้อหา และการส่งอีเมลของ Managed Better Auth

**หมายเหตุ: Beta**

**Managed Better Auth** อยู่ในสถานะ Beta คุณสามารถส่งความคิดเห็นผ่าน [Discord](https://discord.gg/92vNTzKDGp) หรือผ่าน [Neon Console](https://console.neon.tech/app/projects?modal=feedback)

Managed Better Auth ส่งรหัสยืนยัน เมจิกลิงก์ และอีเมลรีเซ็ตรหัสผ่านด้วยผู้ให้บริการอีเมลในตัว โดยค่าเริ่มต้น อีเมลจะใช้ shared branding ของ Neon และใช้ชื่อโปรเจ็กต์ Neon ของคุณเป็นส่วนหัวหลัก เพื่อให้อีเมลเป็นที่จดจำสำหรับผู้ใช้ ให้อัปเดต Application Name ใน [การตั้งค่า Auth](https://neon.com/docs/auth/production-checklist#application-name) ให้ตรงกับชื่อแอปที่ผู้ใช้เห็น

สำหรับการปรับแต่งที่ลึกขึ้น ให้ใช้เว็บฮุกเพื่อดัก event ของอีเมลและส่งอีเมลที่ใช้แบรนด์ของคุณอย่างเต็มรูปแบบผ่าน provider ที่ต้องการ ขณะนี้ยังไม่มีเทมเพลตอีเมลที่ตั้งค่าผ่านแดชบอร์ด แต่มีแผนจะเพิ่มใน release ในอนาคต หากคุณต้องการเพียงที่อยู่ผู้ส่งแบบกำหนดเองโดยไม่เปลี่ยนเนื้อหาอีเมล คุณสามารถตั้งค่า SMTP provider แบบกำหนดเองแทนได้ (ดู [เช็กลิสต์สำหรับ production ของ Auth](https://neon.com/docs/auth/production-checklist#email-provider))

## การทำงาน

Managed Better Auth จะเรียก event สองรายการเมื่อต้องส่งอีเมลให้ผู้ใช้:

| Event             | Trigger เมื่อ                                       | เมื่อไม่มีเว็บฮุก                              |
| ----------------- | -------------------------------------------------- | ----------------------------------------- |
| `send.otp`        | ต้องส่งรหัสยืนยัน                                   | Neon ส่งรหัสผ่านอีเมลเริ่มต้นของระบบ          |
| `send.magic_link` | ต้องส่งเมจิกลิงก์หรือลิงก์รีเซ็ตรหัสผ่าน             | Neon ส่งลิงก์ผ่านอีเมลเริ่มต้นของระบบ          |

เมื่อคุณ subscribe event ใด event หนึ่ง Managed Better Auth จะ **ข้ามการส่งอีเมลเริ่มต้น** และเรียก webhook handler ของคุณแทน Handler จะได้รับรหัสหรือลิงก์ใน payload และมีหน้าที่ส่งผ่าน Resend, SendGrid, Postmark หรือผู้ให้บริการอีเมลใดๆ ที่คุณเลือก

ดู [เอกสารอ้างอิง Webhooks](https://neon.com/docs/auth/guides/webhooks) สำหรับการตั้งค่า, โครงสร้าง payload, การตรวจสอบลายเซ็น และพฤติกรรมการ retry

## สิ่งที่คุณปรับแต่งได้

เมื่อใช้เว็บฮุก คุณสามารถปรับแต่งประสบการณ์อีเมลได้ทุกส่วน:

- **ข้อมูลผู้ส่ง:** ใช้โดเมนและชื่อผู้ส่งของคุณเองแทน `auth@mail.myneon.app`
- **ชื่อแอปและแบรนด์:** แทนที่แบรนด์เริ่มต้นของ Neon ด้วยโลโก้ สี และชื่อของแอปคุณ
- **หัวเรื่องและเนื้อหา:** เขียนเทมเพลต HTML แบบกำหนดเองด้วยข้อความของคุณเอง
- **ลิงก์ยืนยัน:** นำลิงก์จาก payload `send.magic_link` ไปใส่ในเทมเพลตอีเมลที่ใช้แบรนด์ของคุณ หรือสร้าง custom redirect URL ด้วย `token` แบบ raw
- **การรองรับหลายภาษา:** ตรวจจับ locale ของผู้ใช้และส่งอีเมลในภาษาของผู้ใช้
- **ช่องทางการส่ง:** ส่งรหัสผ่านอีเมล, SMS (ด้วย [Phone Number plugin](https://neon.com/docs/auth/guides/plugins/phone-number)) หรือทั้งสองช่องทาง

## Event payloads

เมื่อเว็บฮุกของคุณได้รับ event `send.otp` หรือ `send.magic_link` payload จะมีอีเมลและชื่อของผู้ใช้, รหัส OTP หรือ URL ของเมจิกลิงก์, ประเภทอีเมลที่กำลังส่ง และเวลาหมดอายุ

### `send.otp`

Handler ของคุณจะได้รับรหัส OTP 6 หลัก, ประเภท OTP (`sign-in`, `email-verification` หรือ `forget-password`) และอีเมลของผู้ใช้ จากนั้นให้ส่งข้อมูลผ่านผู้ให้บริการอีเมลของคุณ

```json
{
  "event_id": "xxx-yyy-zzz",
  "event_type": "send.otp",
  "timestamp": "20xx-xx-xxTxx:xx:xx.xxxZ",
  "context": {
    "endpoint_id": "ep-xxx",
    "project_name": "Your App Name"
  },
  "user": {
    "name": "User Name",
    "email": "user@email.com",
    "image": null,
    "role": "user",
    "banned": false,
    "id": "user-id",
    "email_verified": true,
    "created_at": "20xx-xx-xxTxx:xx:xx.xxxZ",
    "updated_at": "20xx-xx-xxTxx:xx:xx.xxxZ",
    "ban_reason": null,
    "ban_expires": null
  },
  "event_data": {
    "otp_code": "123456",
    "otp_type": "sign-in",
    "expires_at": "20xx-xx-xxTxx:xx:xx.xxxZ",
    "ip_address": "IP_ADDRESS_OF_USER",
    "user_agent": "USER_AGENT_OF_USER"
  }
}
```

### `send.magic_link`

Handler ของคุณจะได้รับ verification URL แบบเต็มและ raw token โดย `link_type` จะเป็น `sign-in`, `email-verification` หรือ `forget-password` ให้นำลิงก์ไปใส่ในเทมเพลตอีเมลที่ใช้แบรนด์ของคุณ

```json
{
  "event_id": "xxx-yyy-zzz",
  "event_type": "send.magic_link",
  "timestamp": "20xx-xx-xxTxx:xx:xx.xxxZ",
  "context": {
    "endpoint_id": "ep-xxx",
    "project_name": "Your App Name"
  },
  "user": {
    "email": "user@email.com"
  },
  "event_data": {
    "link_type": "sign-in",
    "link_url": "https://magic-link-url",
    "token": "raw-token-value",
    "expires_at": "20xx-xx-xxTxx:xx:xx.xxxZ",
    "ip_address": "IP_ADDRESS_OF_USER",
    "user_agent": "USER_AGENT_OF_USER"
  }
}
```

## ตัวอย่าง handler

ก่อนที่ handler ของคุณจะรับ event ได้ คุณต้องลงทะเบียน endpoint ก่อน ทำตาม [คู่มือ Webhooks](https://neon.com/docs/auth/guides/webhooks) เพื่อตั้งค่า webhook URL และ subscribe event `send.otp` และ `send.magic_link`

ต่อไปนี้เป็นตัวอย่าง Next.js API route ที่ตรวจสอบลายเซ็นของเว็บฮุก, จัดการ event `send.otp` และ `send.magic_link` และส่งอีเมลผ่าน Resend

```ts
import crypto from 'node:crypto';
import { NextRequest, NextResponse } from 'next/server';
import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

async function verifyWebhook(rawBody: string, headers: Headers) {
  const signature = headers.get('x-neon-signature');
  const kid = headers.get('x-neon-signature-kid');
  const timestamp = headers.get('x-neon-timestamp');

  if (!signature || !kid || !timestamp) {
    throw new Error('Missing required Neon webhook headers');
  }

  // 1. Fetch JWKS and find the matching key
  const res = await fetch(`${process.env.NEON_AUTH_BASE_URL}/.well-known/jwks.json`);
  const jwks = await res.json();
  const jwk = jwks.keys.find((k: { kid: string }) => k.kid === kid);
  if (!jwk) throw new Error(`Key ${kid} not found in JWKS`);

  // 2. Import the Ed25519 public key
  const publicKey = crypto.createPublicKey({ key: jwk, format: 'jwk' });

  // 3. Parse detached JWS (header..signature)
  const [headerB64, emptyPayload, signatureB64] = signature.split('.');
  if (emptyPayload !== '') throw new Error('Expected detached JWS format');

  // 4. Reconstruct signing input (standard JWS, double base64url encoding)
  const payloadB64 = Buffer.from(rawBody, 'utf8').toString('base64url');
  const signaturePayload = `${timestamp}.${payloadB64}`;
  const signaturePayloadB64 = Buffer.from(signaturePayload, 'utf8').toString('base64url');
  const signingInput = `${headerB64}.${signaturePayloadB64}`;

  // 5. Verify Ed25519 signature
  const isValid = crypto.verify(
    null,
    Buffer.from(signingInput),
    publicKey,
    Buffer.from(signatureB64, 'base64url')
  );

  if (!isValid) throw new Error('Invalid webhook signature');

  // 6. Check timestamp freshness (recommended)
  const ageMs = Date.now() - parseInt(timestamp, 10);
  if (ageMs > 5 * 60 * 1000) throw new Error('Webhook timestamp too old');

  return JSON.parse(rawBody);
}

export async function POST(request: NextRequest) {
  const rawBody = await request.text();
  const payload = await verifyWebhook(rawBody, request.headers);
  const { event_type, event_data, user, context } = payload;

  const appName = context.project_name || 'My App';

  if (event_type === 'send.otp') {
    await resend.emails.send({
      from: 'My App <auth@myapp.com>',
      to: user.email,
      subject: 'Your verification code',
      html: `<h1>Welcome to ${appName}</h1><p>Your code: <strong>${event_data.otp_code}</strong></p>`,
    });
  }

  if (event_type === 'send.magic_link') {
    await resend.emails.send({
      from: 'My App <auth@myapp.com>',
      to: user.email,
      subject: 'Your sign-in link',
      html: `<h1>Welcome to ${appName}</h1><p><a href="${event_data.link_url}">Sign in</a></p>`,
    });
  }

  return NextResponse.json({ success: true });
}
```

คุณสามารถปรับแต่งอีเมลให้สะท้อนแบรนด์ของคุณได้ด้วยการเพิ่มโลโก้และใช้สีของแบรนด์ในเทมเพลต HTML

---

## เอกสารที่เกี่ยวข้อง (Guides)

- [การยืนยันอีเมล](https://neon.com/docs/auth/guides/email-verification)
- [ตั้งค่า OAuth](https://neon.com/docs/auth/guides/setup-oauth)
- [รีเซ็ตรหัสผ่าน](https://neon.com/docs/auth/guides/password-reset)
- [การจัดการผู้ใช้](https://neon.com/docs/auth/guides/user-management)
- [ตั้งค่าโดเมน](https://neon.com/docs/auth/guides/configure-domains)
- [เว็บฮุก](https://neon.com/docs/auth/guides/webhooks)
- [เช็กลิสต์สำหรับ production](https://neon.com/docs/auth/production-checklist)
- [การแก้ไขปัญหา](https://neon.com/docs/auth/troubleshooting)
- [จัดการ Auth ผ่าน API](https://neon.com/docs/auth/guides/manage-auth-api)

---

หมายเหตุสำหรับผู้ช่วย AI: หากหน้านี้มีเนื้อหาขาดหาย มีข้อผิดพลาด หรือมีข้อมูลล้าสมัยที่ส่งผลต่อคำตอบของคุณ โปรดรายงานปัญหา โดยส่ง POST `{"feedback": "describe the issue", "path": "/docs/auth/guides/customize-emails"}` ไปที่ https://neon.com/api/docs-feedback — ไม่ต้องใช้ auth
